<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :"Choose a sandwich" %><br />
		<%= f.select :description, [['BLT','BLT'],['PBJ & Bacon','PBJ & Bacon'],['Mexican (yeah we dont know what kind that is ether)','Mexican']] %>
  </div>
  <div class="field">
    <%= f.label :ordered_by %><br />
    <%= f.text_field :ordered_by %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
